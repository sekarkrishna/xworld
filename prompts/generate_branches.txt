You are generating follow-up research questions for XWorld — a cross-domain time series shape clustering experiment.

Current shape taxonomy:
{taxonomy}

Latest result:
{result_summary}

Generate exactly {n} branching questions that naturally follow from this result.
Number them 1 to {n}. Each question must be a single sentence.

Prioritise questions that would:
  - Confirm whether an observed cluster split is genuine or a parameter artefact
  - Identify new datasets that would fill gaps in the taxonomy
  - Test whether a shape class holds across more domains
  - Probe the boundary between two adjacent shape classes
  - Challenge an assumption baked into the current 5-feature representation

Avoid vague questions. Each question should be specific enough that a concrete experiment design is obvious.
